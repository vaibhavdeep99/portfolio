{
  "name": "vaibhav-portfolio-fullstack",
  "version": "1.0.0",
  "description": "Fullstack Portfolio Website with Admin Panel",
  "main": "backend/server.js",
  "scripts": {
    "install-backend": "cmd /c \"cd backend && npm install\"",
    "install-frontend": "cmd /c \"cd frontend && npm install\"",
    "install-all": "cmd /c \"npm run install-backend && npm run install-frontend\"",
    "backend": "node backend/server.js",
    "frontend": "cmd /c \"npm run --prefix frontend dev\"",
    "dev": "npx concurrently \"node backend/server.js\" \"npm run --prefix frontend dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.0",
    "multer": "^1.4.5-lts.1"
  }
}
